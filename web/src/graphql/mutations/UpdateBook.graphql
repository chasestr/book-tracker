mutation UpdateBook($id: Int!, $input: BookInput!) {
  updateBook(id: $id, input: $input) {
    id
    title
    author
    status
    publisher
    pages
    startDate
    finishDate
    notes
    summary
    genre
    rating
    userId
  }
}
