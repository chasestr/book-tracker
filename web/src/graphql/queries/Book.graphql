query Book($id: Int!) {
  book(id: $id) {
    id
    createdAt
    updatedAt
    title
    author
    publisher
    pages
    startDate
    finishDate
    notes
    summary
    genre
    rating
    userId
  }
}
